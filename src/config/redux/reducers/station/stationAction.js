export const setSelectedStation = (station) => ({
    type: "SET_SELECTED_STATION",
    payload: station,
});

export const setStations = (stations) => ({
    type: "SET_STATIONS",
    payload: stations,
});
